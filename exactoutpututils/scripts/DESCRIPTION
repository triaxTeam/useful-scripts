Этот скрипт предназначен для более быстрого пересчета файлов при помощи exactoutput.exe.
Использовать его нужно в случае когда есть файл exactoutput.dat и в нем есть список файлов помеченых как error или warning.
Скрипт выберет все такие имена файлов, затем возьмет эти файлы из папки binfiles или samples которую так же нужно указать
и скопирует во временную папку.
Затем все выбранные файлы пересчитаются и перезапищутся в папки binfiles или samples. Отсанется только закоммитить их.
Какие бывают юзкейсы:
  1. Если не работает XGE (как в моем случае) то можно запустить dryRun на сервере скачать полученный exactoutput.dat файл
    и запустив скрипт посчитать локально у себя только изменившиеся файлы
  2. Если кто то заинтегрейтил новые файлы прямо перед вашим интегрейтом, и они тоже оказались диффернтами. Тоже можно скачать 
    exactoutput.dat файл и пересчитать только изменившиеся
  3. Когда нужно много раз пересчитывать одни и те же изменившиеся файлы. Один раз считаем честно,
  а потом на основе полученного exactoutput.dat файла пересчитываем только дифференты.
  
  Задание параметров скрипта
  Параметры для выполнения задаются при помощи json файла "config.json" имеющего следующие параметры:
    "BinFilePath": - путь к папке с бинфайлами (samples или binfiles или содержащаея их папка)
    "ExectOutputPath": - путь к exactoutput.exe файлу,
    "TxtFile": путь к exactoutput.dat файлу,
    "RegEx" : регулярное выражение которое выбирает имена файлов из TxtFile
              лучше оставить значение по умолчанию - "(?:Error|Warning).*->.*->(.*\\.bin)",
    "FolderWithRecalculated": путь к пакпке exactoutput\\ReleaseUnicode в которой
                              exactoutput.exe создает папки с пересчитанными бинфайлами
    "Actions": - действия которые скрипт должен выполнить:
              Допустимые значения "calculate" - запустить exactoutput.exe  и пересчитать все найденные файлы
                                   "removeTmp" - удалить временную папку в которую копироваллись найденные в  exactoutput.dat файлы
                                   "copySource" - копировать файлы во временную папку если этот парпметр не задан
                                                  значит вы понимаете что временная папка уже создана и в копирование файлов нет нужды
                                   "copyResult" - копировать файлы из папки
                                           exactoutput\\ReleaseUnicode\\<имя папки с пресчитанными файлами> в паки заданные в BinFilePath
    Значение "Actions" по умолчанию ["calculate", "removeTmp", "copySource", "copyResult"] и в общем случае можно его не менять
    
    Запуск скрипта:
    Задав все значения в json файле достаточно просто вызвать команду "python exactOutputFileFromTXTRunner.py"
